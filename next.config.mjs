/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['matrix.droom.in', 'local.api.droompay.in'],
    },
    reactStrictMode: false
  };
  
  export default nextConfig;
  